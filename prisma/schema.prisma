generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("PRISMA_DATABASE_URL")
}

model character {
  id   String  @id @db.Uuid
  name String? @db.VarChar(256)
}
